import { defineConfig } from 'vite';

export default defineConfig({
  // Remove if your app root URL is not a subpath
  // Change depending on your app root URL subpath
  base: '/vite-hello-world-daisyui/',
})
